/* Seed network topology for project/topology.html. Empty by default --
   build your own in the Topology editor (it persists in this browser's
   localStorage from then on). */
const topologyConfig = {
  "links": [],
  "nodes": []
};
